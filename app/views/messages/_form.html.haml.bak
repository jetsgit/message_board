/ AJAX message
- modal ||= false
- remote =  modal ? true : false

#message-form
= simple_form_for(@message, remote: true, :url => @message.new_record? ? url_for(action: 'create', controller: 'messages') : url_for(action: 'update', controller: 'messages')) do |f|         
  = f.error_notification
  .form-inputs
    .form-group.col-md-10.col-md-offset-1
      = f.input :title, wrapper: :vertical_input_group do
        = f.input_field :title, class: "form-control"
        %span.help-block
    .form-group.col-md-10.col-md-offset-1
      = f.input :content, wrapper: :vertical_input_group do
        = f.input_field :content, class: "form-control"
        %span.help-block
  .form-actions.col-md-6.pull-right
    %button.btn.btn-close( type='button' data-dismiss='modal'  ) Close
    = f.button :submit, class: "btn btn-primary"
